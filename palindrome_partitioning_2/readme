凡是求最优解的，一般都是走DP的路线。
先求DP的递推公式。
DP[i,n]，区间i，n之间最小的cut数，n为字符串的长度
二维退化为一维
DP[i]=min(1+DP[j+1]), i=<j<=n;
DP[i]表示i到n的字符串中最小的cut。
然后还要动态的判断是否回文
isP[i][j]。表示i到j的字符串是回文
isP[i][j] = s[i]s[j] && isP[i-1][j-1]
